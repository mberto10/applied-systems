# Dynamic Workflows

References and workflow-shape notes for building custom harnesses with dynamic workflows.

This section will hold the contract-to-runtime mapping used in the launch essay:

- what the eval contract declares
- what the workflow runtime derives
- which decisions are fixed before a run
- which decisions return to a human between runs

Status: scaffold for the launch package.
